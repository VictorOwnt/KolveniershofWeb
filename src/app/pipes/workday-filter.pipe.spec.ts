import { WorkdayFilterPipe } from './workday-filter.pipe';

describe('WorkdayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkdayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
